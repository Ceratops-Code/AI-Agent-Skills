{
  "name": "ceratops-codex-skills",
  "private": true,
  "description": "Development validation tooling for Ceratops Codex skills.",
  "scripts": {
    "lint:markdown": "markdownlint \"**/*.md\" --ignore node_modules --config scripts/.markdownlint.json"
  },
  "devDependencies": {
    "markdownlint-cli": "0.49.1"
  }
}
